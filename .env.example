# Environment Configuration Example
# Copy this file to .env and configure with your actual values
# NEVER commit .env file with real credentials to repository

###> symfony/framework-bundle ###
APP_ENV=prod
# Generate strong secret: openssl rand -base64 32
APP_SECRET=e185ad5ca46b7d5db856252612422d4e_CHANGE_THIS
###< symfony/framework-bundle ###

###> doctrine/doctrine-bundle ###
# Database configuration
# IMPORTANT: Change default credentials!
# For production, use strong passwords and consider SSL connections
DATABASE_URL="postgresql://nn_user:USE_STRONG_PASS_MIN_16_CHARS@127.0.0.1:5432/nowa_nadzieja_crm?serverVersion=16&charset=utf8&sslmode=require"
###< doctrine/doctrine-bundle ###

###> symfony/messenger ###
# Choose one of the transports below
# MESSENGER_TRANSPORT_DSN=amqp://guest:guest@localhost:5672/%2f/messages
# MESSENGER_TRANSPORT_DSN=redis://localhost:6379/messages
MESSENGER_TRANSPORT_DSN=doctrine://default?auto_setup=0
###< symfony/messenger ###

###> symfony/mailer ###
# Production SMTP configuration
# Replace with your actual SMTP settings
MAILER_DSN=smtp://username:password@smtp.example.com:587
# For development you can use:
# MAILER_DSN=smtp://localhost:1025
###< symfony/mailer ###

###> nelmio/security-bundle ###
# Security configuration
FORCE_HTTPS=true
CSP_REPORT_URI=/security/csp-report
###< nelmio/security-bundle ###

###> fixtures (development only) ###
# Strong password for admin user in fixtures (development only)
# Generate with: openssl rand -base64 32
FIXTURE_ADMIN_PASSWORD=GENERATE_STRONG_PASSWORD_FOR_DEV
###< fixtures ###

###> Additional Security Settings ###
# Photo upload directory (ensure proper permissions)
PHOTOS_DIRECTORY=%kernel.project_dir%/public/uploads/photos

# Session security
SESSION_COOKIE_DOMAIN=your-domain.com
SESSION_COOKIE_PATH=/

# Rate limiting for API (if implemented)
# RATE_LIMIT_REQUESTS_PER_MINUTE=60
###< Additional Security Settings ###

###> Production Deployment ###
# These should be set in production environment
# DATABASE_SSL_MODE=require
# DATABASE_SSL_CERT=/path/to/client-cert.pem
# DATABASE_SSL_KEY=/path/to/client-key.pem
# DATABASE_SSL_CA=/path/to/ca-cert.pem

# Logging level for production
# LOG_LEVEL=warning

# Cache configuration for production
# CACHE_DSN=redis://localhost:6379/0
###< Production Deployment ###