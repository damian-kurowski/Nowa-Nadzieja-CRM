<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <title>Zapomniałem hasła - CRM Nowa Nadzieja</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #1e40af;
            --primary-light: #3b82f6;
            --primary-dark: #1d4ed8;
            --success: #10b981;
            --danger: #ef4444;
            --warning: #f59e0b;
            --gray-100: #f3f4f6;
            --gray-200: #e5e7eb;
            --gray-300: #d1d5db;
            --gray-400: #9ca3af;
            --gray-500: #6b7280;
            --gray-600: #4b5563;
            --gray-700: #374151;
            --gray-800: #1f2937;
            --gray-900: #111827;
        }

        * {
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            position: relative;
            overflow-x: hidden;
        }

        body::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image:
                radial-gradient(circle at 20% 80%, rgba(255,255,255,0.1) 2px, transparent 2px),
                radial-gradient(circle at 80% 20%, rgba(255,255,255,0.1) 2px, transparent 2px),
                radial-gradient(circle at 40% 40%, rgba(255,255,255,0.08) 1px, transparent 1px);
            background-size: 100px 100px, 80px 80px, 40px 40px;
            animation: float-particles 20s ease-in-out infinite;
            pointer-events: none;
        }

        @keyframes float-particles {
            0%, 100% {
                transform: translateY(0px) rotate(0deg);
                opacity: 0.7;
            }
            33% {
                transform: translateY(-10px) rotate(0.5deg);
                opacity: 1;
            }
            66% {
                transform: translateY(-5px) rotate(-0.5deg);
                opacity: 0.8;
            }
        }

        body::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image:
                linear-gradient(90deg, rgba(255, 255, 255, 0.05) 1px, transparent 1px),
                linear-gradient(rgba(255, 255, 255, 0.05) 1px, transparent 1px);
            background-size: 40px 40px;
            opacity: 0.6;
            z-index: 1;
        }

        .login-container {
            position: relative;
            z-index: 10;
            width: 100%;
        }

        .login-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            box-shadow:
                0 20px 40px -10px rgba(0, 0, 0, 0.2),
                0 10px 20px -5px rgba(0, 0, 0, 0.1),
                inset 0 1px 0 rgba(255, 255, 255, 0.3);
            border-radius: 20px;
            overflow: hidden;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            max-width: 540px;
            margin: 0 auto;
            position: relative;
        }

        .login-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 1px;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
            border-radius: 20px 20px 0 0;
        }

        .login-card:hover {
            transform: translateY(-5px);
            box-shadow:
                0 30px 60px -10px rgba(0, 0, 0, 0.25),
                0 15px 30px -5px rgba(0, 0, 0, 0.15),
                inset 0 1px 0 rgba(255, 255, 255, 0.4);
            background: rgba(255, 255, 255, 0.98);
        }

        .card-header {
            background: transparent;
            color: var(--gray-800);
            border: none;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            padding: 2.5rem 2.5rem 1.5rem;
            text-align: center;
            position: relative;
        }

        .logo-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
            background: linear-gradient(135deg, #667eea, #764ba2);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
            display: block;
            filter: drop-shadow(0 2px 4px rgba(102, 126, 234, 0.3));
        }

        .card-title {
            font-weight: 700;
            font-size: 1.75rem;
            margin: 0;
            color: var(--gray-900);
            letter-spacing: -0.025em;
        }

        .card-subtitle {
            font-weight: 500;
            font-size: 0.95rem;
            margin-top: 0.5rem;
            color: var(--gray-600);
            line-height: 1.5;
        }

        .card-body {
            padding: 2rem 2.5rem 2.5rem;
        }

        .form-label {
            font-weight: 500;
            color: var(--gray-700);
            margin-bottom: 0.5rem;
            font-size: 0.9rem;
        }

        .form-control, .form-select {
            border: 1.5px solid rgba(255, 255, 255, 0.3);
            border-radius: 12px;
            padding: 0.75rem 1rem;
            font-size: 1rem;
            transition: all 0.3s ease;
            background: rgba(255, 255, 255, 0.9);
            color: var(--gray-900);
            box-shadow: inset 0 1px 2px rgba(0, 0, 0, 0.1);
        }

        .form-control:focus, .form-select:focus {
            border-color: rgba(102, 126, 234, 0.8);
            box-shadow:
                0 0 0 4px rgba(102, 126, 234, 0.15),
                inset 0 1px 2px rgba(0, 0, 0, 0.05);
            background: rgba(255, 255, 255, 0.95);
            outline: none;
        }

        textarea.form-control {
            min-height: 120px;
            resize: vertical;
        }

        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
            border-radius: 14px;
            padding: 1rem 2rem;
            font-weight: 600;
            font-size: 1rem;
            color: white;
            transition: all 0.4s ease;
            box-shadow:
                0 8px 20px rgba(102, 126, 234, 0.4),
                inset 0 1px 0 rgba(255, 255, 255, 0.2);
            text-shadow: 0 1px 2px rgba(0, 0, 0, 0.1);
        }

        .btn-primary:hover {
            transform: translateY(-3px) scale(1.02);
            box-shadow:
                0 12px 30px rgba(102, 126, 234, 0.5),
                inset 0 1px 0 rgba(255, 255, 255, 0.3);
            background: linear-gradient(135deg, #5a6fd8 0%, #6b5b95 100%);
        }

        .btn-secondary {
            background: rgba(255, 255, 255, 0.9);
            border: 1.5px solid var(--gray-300);
            border-radius: 14px;
            padding: 1rem 2rem;
            font-weight: 600;
            font-size: 1rem;
            color: var(--gray-700);
            transition: all 0.3s ease;
        }

        .btn-secondary:hover {
            background: rgba(255, 255, 255, 1);
            border-color: var(--gray-400);
            color: var(--gray-900);
            transform: translateY(-2px);
        }

        .alert {
            border-radius: 12px;
            border: none;
            padding: 1rem 1.25rem;
            margin-bottom: 1.5rem;
            font-weight: 500;
            animation: slideDown 0.3s ease;
        }

        @keyframes slideDown {
            from {
                opacity: 0;
                transform: translateY(-10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .alert-success {
            background: rgba(16, 185, 129, 0.1);
            color: var(--success);
            border-left: 4px solid var(--success);
        }

        .info-box {
            background: linear-gradient(135deg, rgba(102, 126, 234, 0.1) 0%, rgba(118, 75, 162, 0.1) 100%);
            border: 1px solid rgba(102, 126, 234, 0.3);
            border-radius: 12px;
            padding: 1.25rem;
            margin-bottom: 1.5rem;
        }

        .info-box p {
            margin: 0;
            color: var(--gray-700);
            font-size: 0.9rem;
            line-height: 1.6;
        }

        .info-box i {
            color: #667eea;
            margin-right: 0.5rem;
        }

        @media (max-width: 768px) {
            body {
                padding: 1rem;
            }

            .login-card {
                margin: 1rem;
                max-width: none;
            }

            .card-header {
                padding: 1.5rem 1.5rem 1rem;
            }

            .card-body {
                padding: 2rem 1.5rem 1.5rem;
            }

            .logo-icon {
                font-size: 2rem;
            }

            .card-title {
                font-size: 1.5rem;
            }
        }
    </style>
</head>

<body>
    <div class="login-container">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-12">
                    <div class="login-card">
                        <div class="card-header text-center">
                            <div class="logo-icon">
                                <i class="fas fa-key"></i>
                            </div>
                            <h1 class="card-title">Zapomniałem hasła</h1>
                            <p class="card-subtitle">Wypełnij formularz, a administrator skontaktuje się z Tobą</p>
                        </div>

                        <div class="card-body">
                            {% for message in app.flashes('success') %}
                                <div class="alert alert-success" role="alert">
                                    <i class="fas fa-check-circle me-2"></i>
                                    {{ message }}
                                </div>
                            {% endfor %}

                            <div class="info-box">
                                <p>
                                    <i class="fas fa-info-circle"></i>
                                    Twoja prośba o reset hasła zostanie wysłana bezpośrednio do administratora systemu.
                                    Skontaktujemy się z Tobą najszybciej jak to możliwe.
                                </p>
                            </div>

                            <form method="post" action="{{ path('password_reset_request') }}" id="resetForm">
                                <div class="mb-3">
                                    <label for="email" class="form-label">
                                        <i class="fas fa-envelope me-1"></i>
                                        Adres e-mail
                                    </label>
                                    <input type="email"
                                           class="form-control"
                                           id="email"
                                           name="email"
                                           placeholder="twoj@email.pl"
                                           required>
                                    <small class="text-muted">Adres e-mail przypisany do Twojego konta</small>
                                </div>

                                <div class="mb-3">
                                    <label for="full_name" class="form-label">
                                        <i class="fas fa-user me-1"></i>
                                        Imię i nazwisko
                                    </label>
                                    <input type="text"
                                           class="form-control"
                                           id="full_name"
                                           name="full_name"
                                           placeholder="Jan Kowalski"
                                           required>
                                </div>

                                <div class="mb-3">
                                    <label for="phone" class="form-label">
                                        <i class="fas fa-phone me-1"></i>
                                        Numer telefonu
                                    </label>
                                    <input type="tel"
                                           class="form-control"
                                           id="phone"
                                           name="phone"
                                           placeholder="+48 123 456 789"
                                           required>
                                </div>

                                <div class="mb-3">
                                    <label for="okreg" class="form-label">
                                        <i class="fas fa-map-marked-alt me-1"></i>
                                        Nazwa okręgu
                                    </label>
                                    <input type="text"
                                           class="form-control"
                                           id="okreg"
                                           name="okreg"
                                           placeholder="Np. Okręg Warszawski"
                                           required>
                                    <small class="text-muted">Podaj nazwę okręgu, do którego należysz</small>
                                </div>

                                <div class="mb-4">
                                    <label for="additional_info" class="form-label">
                                        <i class="fas fa-comment-alt me-1"></i>
                                        Dodatkowe informacje (opcjonalne)
                                    </label>
                                    <textarea class="form-control"
                                              id="additional_info"
                                              name="additional_info"
                                              rows="3"
                                              placeholder="Możesz dodać dodatkowe informacje, które pomogą administratorowi zidentyfikować Twoje konto"></textarea>
                                </div>

                                <input type="hidden" name="_csrf_token" value="{{ csrf_token('password_reset') }}">

                                <div class="d-grid gap-2 mb-3">
                                    <button class="btn btn-primary" type="submit">
                                        <i class="fas fa-paper-plane me-2"></i>
                                        Wyślij prośbę o reset hasła
                                    </button>
                                    <a href="{{ path('login') }}" class="btn btn-secondary">
                                        <i class="fas fa-arrow-left me-2"></i>
                                        Powrót do logowania
                                    </a>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
