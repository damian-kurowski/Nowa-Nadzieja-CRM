{% extends 'base.html.twig' %}

{% block title %}Wyznacz Obserwatora Zebrania - CRM Nowa Nadzieja{% endblock %}

{% block page_title %}Wyznacz Obserwatora Zebrania Oddziału{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .step-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 16px;
            padding: 2rem;
            margin-bottom: 2rem;
            color: white;
        }

        .step-card {
            background: var(--bg-primary);
            border: 1px solid var(--border-color);
            border-radius: 16px;
            padding: 2rem;
            box-shadow: 0 4px 16px var(--shadow-light);
            margin-bottom: 2rem;
        }

        .oddzial-card {
            border: 2px solid var(--border-color);
            border-radius: 12px;
            padding: 1rem;
            margin-bottom: 1rem;
            cursor: pointer;
            transition: all 0.3s ease;
            background: var(--bg-primary);
        }

        .oddzial-card:hover {
            border-color: #667eea;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px var(--shadow-light);
        }

        .oddzial-card.selected {
            border-color: #667eea;
            background: linear-gradient(135deg, rgba(102, 126, 234, 0.1), rgba(118, 75, 162, 0.05));
        }

        .member-card {
            border: 2px solid var(--border-color);
            border-radius: 12px;
            padding: 1rem;
            margin-bottom: 1rem;
            cursor: pointer;
            transition: all 0.3s ease;
            background: var(--bg-primary);
        }

        .member-card:hover {
            border-color: #667eea;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px var(--shadow-light);
        }

        .member-card.selected {
            border-color: #667eea;
            background: linear-gradient(135deg, rgba(102, 126, 234, 0.1), rgba(118, 75, 162, 0.05));
        }

        .member-avatar {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            background: linear-gradient(135deg, #667eea, #764ba2);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: bold;
            font-size: 1.2rem;
        }

        .oddzial-icon {
            width: 60px;
            height: 60px;
            border-radius: 12px;
            background: linear-gradient(135deg, #667eea, #764ba2);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.5rem;
        }

        .member-info h5 {
            margin-bottom: 0.25rem;
            color: var(--text-primary);
        }

        .member-info .text-muted {
            font-size: 0.875rem;
        }

        .step-info {
            background: linear-gradient(135deg, rgba(102, 126, 234, 0.1), rgba(118, 75, 162, 0.05));
            border: 1px solid rgba(102, 126, 234, 0.2);
            border-radius: 12px;
            padding: 1.5rem;
            margin-bottom: 2rem;
        }

        .btn-primary-gradient {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            border: none;
            padding: 0.75rem 2rem;
            border-radius: 8px;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .btn-primary-gradient:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
            color: white;
        }
    </style>
{% endblock %}

{% block body %}
    <div class="step-header">
        <h1 class="mb-2">
            <i class="fas fa-eye me-2"></i>
            Rozpocznij Zebranie Oddziału
        </h1>
        <p class="mb-0 opacity-90">
            Wyznacz obserwatora zebrania dla wybranego oddziału
        </p>
    </div>

    <div class="step-info">
        <h5 class="mb-3">
            <i class="fas fa-info-circle me-2"></i>
            Informacje o obserwatorze
        </h5>
        <ul class="mb-0">
            <li><strong>Obserwator zebrania</strong> to osoba wyznaczana przez Sekretarza Okręgu</li>
            <li>Musi być członkiem tego samego okręgu co oddział</li>
            <li>Będzie nadzorował przebieg zebrania i zarządzał jego krokami</li>
            <li>Otrzyma tymczasową rolę ROLE_OBSERWATOR_ZEBRANIA na czas zebrania</li>
            <li>Po zakończeniu zebrania rola zostanie automatycznie odebrana</li>
        </ul>
    </div>

    <form method="post" class="step-card">
        <h4 class="mb-4">
            <i class="fas fa-building me-2"></i>
            Krok 1: Wybierz oddział
        </h4>

        {% if oddzialy|length > 0 %}
            <div class="mb-4">
                <input type="text" class="form-control" id="searchOddzial"
                       placeholder="Wyszukaj oddział...">
            </div>

            <div class="oddzialy-list mb-4" style="max-height: 400px; overflow-y: auto;">
                {% for oddzial in oddzialy %}
                    <div class="oddzial-card" data-id="{{ oddzial.id }}"
                         data-name="{{ oddzial.nazwa|lower }}">
                        <div class="d-flex align-items-center">
                            <div class="oddzial-icon me-3">
                                <i class="fas fa-building"></i>
                            </div>
                            <div class="flex-grow-1">
                                <h5>{{ oddzial.nazwa }}</h5>
                                <span class="text-muted">
                                    {% if oddzial.siedziba %}
                                        <i class="fas fa-map-marker-alt me-1"></i>{{ oddzial.siedziba }}
                                    {% endif %}
                                    {% if oddzial.powiat %}
                                        {% if oddzial.siedziba %} | {% endif %}
                                        <i class="fas fa-map me-1"></i>{{ oddzial.powiat }}
                                    {% endif %}
                                    {% if oddzial.czlonkowie|length > 0 %}
                                        | <i class="fas fa-users ms-2 me-1"></i>{{ oddzial.czlonkowie|length }} członków
                                    {% endif %}
                                </span>
                            </div>
                            <div>
                                <input type="radio" name="oddzial" value="{{ oddzial.id }}"
                                       class="form-check-input oddzial-radio" required>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% else %}
            <div class="alert alert-warning">
                <i class="fas fa-exclamation-triangle me-2"></i>
                W Twoim okręgu nie ma przypisanych oddziałów.
            </div>
        {% endif %}

        <hr class="my-4">

        <h4 class="mb-4">
            <i class="fas fa-user-check me-2"></i>
            Krok 2: Wybierz obserwatora
        </h4>

        {% if potencjalni_obserwatorzy|length > 0 %}
            <div class="mb-3">
                <input type="text" class="form-control" id="searchMember"
                       placeholder="Wyszukaj obserwatora po imieniu lub nazwisku...">
            </div>

            <div class="members-list" style="max-height: 500px; overflow-y: auto;">
                {% for czlonek in potencjalni_obserwatorzy %}
                    <div class="member-card" data-id="{{ czlonek.id }}"
                         data-name="{{ czlonek.fullName|lower }}">
                        <div class="d-flex align-items-center">
                            <div class="member-avatar me-3">
                                {{ czlonek.imie|first|upper }}{{ czlonek.nazwisko|first|upper }}
                            </div>
                            <div class="member-info flex-grow-1">
                                <h5>{{ czlonek.fullName }}</h5>
                                <span class="text-muted">
                                    {% if czlonek.email %}
                                        <i class="fas fa-envelope me-1"></i>{{ czlonek.email }}
                                    {% endif %}
                                    {% if czlonek.telefon %}
                                        <i class="fas fa-phone ms-3 me-1"></i>{{ czlonek.telefon }}
                                    {% endif %}
                                </span>
                                {% if czlonek.roles|length > 1 %}
                                    <div class="mt-1">
                                        {% if 'ROLE_SEKRETARZ_OKREGU' in czlonek.roles %}
                                            <span class="badge bg-primary">Sekretarz Okręgu</span>
                                        {% endif %}
                                        {% if 'ROLE_PREZES_OKREGU' in czlonek.roles %}
                                            <span class="badge bg-success">Prezes Okręgu</span>
                                        {% endif %}
                                        {% if 'ROLE_WICEPREZES_OKREGU' in czlonek.roles %}
                                            <span class="badge bg-info">Wiceprezes Okręgu</span>
                                        {% endif %}
                                    </div>
                                {% endif %}
                            </div>
                            <div>
                                <input type="radio" name="obserwator" value="{{ czlonek.id }}"
                                       class="form-check-input" required>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% else %}
            <div class="alert alert-warning">
                <i class="fas fa-exclamation-triangle me-2"></i>
                Brak potencjalnych obserwatorów w okręgu.
            </div>
        {% endif %}

        <div class="d-flex justify-content-between mt-4">
            <a href="{{ path('zebranie_oddzialu_index') }}" class="btn btn-outline-secondary">
                <i class="fas fa-arrow-left me-2"></i>
                Anuluj
            </a>
            {% if oddzialy|length > 0 and potencjalni_obserwatorzy|length > 0 %}
                <button type="submit" class="btn btn-primary-gradient">
                    <i class="fas fa-check me-2"></i>
                    Rozpocznij zebranie
                </button>
            {% endif %}
        </div>
    </form>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        // Obsługa wyboru oddziału
        document.querySelectorAll('.oddzial-card').forEach(card => {
            card.addEventListener('click', function() {
                // Usuń zaznaczenie z innych
                document.querySelectorAll('.oddzial-card').forEach(c => {
                    c.classList.remove('selected');
                });

                // Zaznacz tę kartę
                this.classList.add('selected');

                // Zaznacz radio button
                const radio = this.querySelector('input[type="radio"]');
                if (radio) {
                    radio.checked = true;
                }
            });
        });

        // Obsługa wyboru obserwatora
        document.querySelectorAll('.member-card').forEach(card => {
            card.addEventListener('click', function() {
                // Usuń zaznaczenie z innych
                document.querySelectorAll('.member-card').forEach(c => {
                    c.classList.remove('selected');
                });

                // Zaznacz tę kartę
                this.classList.add('selected');

                // Zaznacz radio button
                const radio = this.querySelector('input[type="radio"]');
                if (radio) {
                    radio.checked = true;
                }
            });
        });

        // Wyszukiwarka oddziałów
        const searchOddzial = document.getElementById('searchOddzial');
        if (searchOddzial) {
            searchOddzial.addEventListener('input', function() {
                const searchTerm = this.value.toLowerCase();

                document.querySelectorAll('.oddzial-card').forEach(card => {
                    const name = card.dataset.name;
                    if (name.includes(searchTerm)) {
                        card.style.display = 'block';
                    } else {
                        card.style.display = 'none';
                    }
                });
            });
        }

        // Wyszukiwarka obserwatorów
        const searchMember = document.getElementById('searchMember');
        if (searchMember) {
            searchMember.addEventListener('input', function() {
                const searchTerm = this.value.toLowerCase();

                document.querySelectorAll('.member-card').forEach(card => {
                    const name = card.dataset.name;
                    if (name.includes(searchTerm)) {
                        card.style.display = 'block';
                    } else {
                        card.style.display = 'none';
                    }
                });
            });
        }
    </script>
{% endblock %}
