<!-- Modal Wyznaczenia Protokolanta -->
<div class="modal fade" id="protokolantModal" tabindex="-1" aria-labelledby="protokolantModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header bg-primary text-white">
                <h5 class="modal-title" id="protokolantModalLabel">
                    <i class="fas fa-pen"></i>
                    Wyznacz Protokolanta Zebrania
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form action="{{ path('zebranie_oddzialu_wyznacz_protokolanta', {'id': zebranie.id}) }}" method="POST">
                <div class="modal-body">
                    <div class="alert alert-info">
                        <i class="fas fa-info-circle"></i>
                        Protokolant będzie odpowiedzialny za sporządzenie protokołu z zebrania i współuczestnictwo w wyborze przewodniczącego zebrania.
                    </div>
                    
                    <div class="mb-3">
                        <label for="protokolant" class="form-label">Wybierz Protokolanta</label>
                        <select class="form-select" id="protokolant" name="protokolant" required>
                            <option value="">-- Wybierz członka --</option>
                            {% for member in availableMembers %}
                                {% if member.id != zebranie.obserwator.id %}
                                    <option value="{{ member.id }}">
                                        {{ member.fullName }} (ID: {{ member.id }})
                                    </option>
                                {% endif %}
                            {% endfor %}
                        </select>
                        <small class="form-text text-muted">
                            Protokolant musi być członkiem oddziału, ale nie może być jednocześnie obserwatorem.
                        </small>
                    </div>
                    
                    <input type="hidden" name="_token" value="{{ csrf_token('wyznacz_protokolanta' ~ zebranie.id) }}">
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                        <i class="fas fa-times"></i> Anuluj
                    </button>
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-check"></i> Wyznacz Protokolanta
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>